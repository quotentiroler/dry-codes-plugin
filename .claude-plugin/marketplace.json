{
  "version": 1,
  "plugins": [
    {
      "name": "dry-codes",
      "source": "./dry-codes",
      "description": "Reuse before you write — search the Dry Codes public corpus (hosted MCP) for existing code and docs to reuse, and catch near-duplicates. Bundles the dry-codes skill + the public /mcp/public server (zero config)."
    }
  ]
}
